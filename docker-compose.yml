version: '3.4'

services:
  logsmithmonitor:
    image: logsmithmonitor
    build:
      context: .
      dockerfile: ./Dockerfile
    environment:
      NODE_ENV: production
    ports:
      - 8050:8050
